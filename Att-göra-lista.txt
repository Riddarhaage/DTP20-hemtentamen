Att Göra:
prio1:

Ändra kommandot "lista" så att den endast visar det som är satt till "active". (klar)
Ändra "lista allt" så att den gör det som "lista" gör i nuläget. (klar)
Implementera kommandot "beskriv", den ska fungera som "lista" plus att den även skriver ut task-description. (klar)
Implementera kommandot "ny" för genom konsolen lägga till ytterligar uppgifter i listan. (klar)
Implementera kommandot "ladda" för att ladda "todo.lis". (klar)
Implementera kommandot "spara" för att spara ändringar som gjorts i listan. (klar)
Implementera kommandot "aktivera /uppgift/" som ändrar status till "active" på den uppgift som anges. (klar)
Implemendera kommandot "klar /uppgift/" som ändrar status till "ready" på den uppgift som anges. (klar)
Implementera kommandot "vänta /uppgift/" som ändrar status till "waiting" på den uppgift som anges. (klar)
För varje nytt implementerat kommando så ska de även läggas till i hjälp-texten som syns i början när programmet körs. (klar)


Prio2:

(vg)Implementera kommando "lista väntande" som listar alla uppgifter med status satt till "väntande" (klar)
(vg)Implementera kommando "lista klara" som listar alla uppgifter med status satt till "avklarad" (klar)
(vg)Implementera kommando "beskriv allt" som listar alla uppgfifter och skrivet ut "beskrivning" till dem. (klar)
(vg)Implementera kommando "ladda /fil/" ladda lista från en fil. (klar)
(vg)Implementera kommando "spara /fil/" spara uppgifterna på ny fil med angivet filnamn. (klar)
(vg)Implementera kommando "ny /uppgift/" som skapar en ny uppgift där namnet sätts som det man anger istället för "/uppgift/".
refaktorera och städa upp kod. (pågående)